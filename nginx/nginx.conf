events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    
    # Logging format
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;
    
    # Gzip Settings
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=web:10m rate=20r/s;

    # SSL Configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Security headers
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";

    # Upstream servers for API
    upstream api_production {
        server localhost:3001;
    }
    
    upstream api_staging {
        server localhost:3002;
    }
    
    upstream api_sandbox {
        server localhost:3003;
    }

    # Upstream servers for Web Apps
    upstream agent_production {
        server localhost:3011;
    }
    
    upstream agent_staging {
        server localhost:3012;
    }
    
    upstream agent_sandbox {
        server localhost:3013;
    }

    upstream distributor_production {
        server localhost:3021;
    }
    
    upstream distributor_staging {
        server localhost:3022;
    }
    
    upstream distributor_sandbox {
        server localhost:3023;
    }

    upstream business_production {
        server localhost:3031;
    }
    
    upstream business_staging {
        server localhost:3032;
    }
    
    upstream business_sandbox {
        server localhost:3033;
    }

    upstream admin_production {
        server localhost:3041;
    }
    
    upstream admin_staging {
        server localhost:3042;
    }
    
    upstream admin_sandbox {
        server localhost:3043;
    }

    upstream lender_production {
        server localhost:3051;
    }
    
    upstream lender_staging {
        server localhost:3052;
    }
    
    upstream lender_sandbox {
        server localhost:3053;
    }

    # Shared monitoring services
    upstream grafana {
        server localhost:3100;
    }

    upstream minio_console {
        server localhost:9001;
    }

    # API Servers
    server {
        listen 80;
        server_name api.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name api.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/api.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/api.cachconnect.co.ke.key;

        location / {
            limit_req zone=api burst=20 nodelay;
            proxy_pass http://api_production;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_read_timeout 86400;
        }
    }

    server {
        listen 80;
        server_name api.staging.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name api.staging.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/api.staging.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/api.staging.cachconnect.co.ke.key;

        location / {
            limit_req zone=api burst=20 nodelay;
            proxy_pass http://api_staging;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_read_timeout 86400;
        }
    }

    server {
        listen 80;
        server_name api.sandbox.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name api.sandbox.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/api.sandbox.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/api.sandbox.cachconnect.co.ke.key;

        location / {
            limit_req zone=api burst=20 nodelay;
            proxy_pass http://api_sandbox;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_read_timeout 86400;
        }
    }

    # Agent Web App
    server {
        listen 80;
        server_name agents.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name agents.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/agents.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/agents.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://agent_production;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name agents.staging.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name agents.staging.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/agents.staging.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/agents.staging.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://agent_staging;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name agents.sandbox.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name agents.sandbox.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/agents.sandbox.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/agents.sandbox.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://agent_sandbox;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # Distributor Web App
    server {
        listen 80;
        server_name distributors.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name distributors.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/distributors.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/distributors.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://distributor_production;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name distributors.staging.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name distributors.staging.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/distributors.staging.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/distributors.staging.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://distributor_staging;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name distributors.sandbox.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name distributors.sandbox.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/distributors.sandbox.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/distributors.sandbox.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://distributor_sandbox;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # Business Web App
    server {
        listen 80;
        server_name business.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name business.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/business.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/business.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://business_production;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name business.staging.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name business.staging.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/business.staging.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/business.staging.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://business_staging;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name business.sandbox.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name business.sandbox.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/business.sandbox.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/business.sandbox.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://business_sandbox;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # Admin Web App
    server {
        listen 80;
        server_name admin.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name admin.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/admin.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/admin.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://admin_production;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name admin.staging.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name admin.staging.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/admin.staging.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/admin.staging.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://admin_staging;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name admin.sandbox.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name admin.sandbox.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/admin.sandbox.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/admin.sandbox.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://admin_sandbox;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # Lender Web App
    server {
        listen 80;
        server_name lenders.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name lenders.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/lenders.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/lenders.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://lender_production;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name lenders.staging.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name lenders.staging.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/lenders.staging.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/lenders.staging.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://lender_staging;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    server {
        listen 80;
        server_name lenders.sandbox.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name lenders.sandbox.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/lenders.sandbox.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/lenders.sandbox.cachconnect.co.ke.key;

        location / {
            limit_req zone=web burst=30 nodelay;
            proxy_pass http://lender_sandbox;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # Shared Monitoring Services
    server {
        listen 80;
        server_name monitoring.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name monitoring.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/monitoring.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/monitoring.cachconnect.co.ke.key;

        location / {
            proxy_pass http://grafana;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # MinIO Console
    server {
        listen 80;
        server_name storage.cachconnect.co.ke;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name storage.cachconnect.co.ke;
        
        ssl_certificate /etc/nginx/certs/storage.cachconnect.co.ke.crt;
        ssl_certificate_key /etc/nginx/certs/storage.cachconnect.co.ke.key;

        location / {
            proxy_pass http://minio_console;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }
}