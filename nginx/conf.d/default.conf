server {
    listen 80;
    server_name localhost;

    location / {
        return 200 "Cach Connect Infrastructure is running!";
        add_header Content-Type text/plain;
    }

    location /health {
        return 200 "OK";
        add_header Content-Type text/plain;
    }
}
